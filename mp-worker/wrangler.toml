name = "finanza-mp-proxy"
main = "src/index.js"
compatibility_date = "2026-05-01"

# Variáveis sensíveis vão como secrets (não aqui)
# MP_ACCESS_TOKEN será setada como secret no wrangler

# Opcional: rota customizada se o domínio estiver na Cloudflare
# routes = [
#   { pattern = "mp-proxy.finanzaai.tech/*", zone_id = "SEU_ZONE_ID" }
# ]
