
node_modules
dist
.env
.env.local
.DS_Store
coverage
.vercel
.env*.local

# IDE
.vscode
.idea

# Logs
*.log

# BMad
**/.opencode/
_bmad-output/

# Agent skills (nested git repo breaks CI/CD submodule cleanup)
mobile/.agent/skills/
_bmad/
BACKLOG.md
BMAD-GUIDE.md
INDEX.md
opencode.json

# Scripts de migração
backend/src/migrate-*.ts
backend/fix_*.js
backend/src/fix_*.ts

# Screenshots
Captura de tela*.png

# Deploy artifacts
mobile/deploy_apk/

# APK / signing sidecars / compressed builds (too large or generated; deploy outside git)
*.apk
*.apk.gz
*.apk.idsig
*.idsig

# Start script
start.sh

# AI agent artifacts
**/.agent/

# One-off fix scripts (not part of the project)
fix_*.js

# Audit/docs/generated marketing pages that don't belong in repo
auditoria-*.md
AGENTS.md
social-media-posts.html
social-posts/

# Plans (keep locally, not in repo)
docs/plans/

# __pycache__
__pycache__/

# === NON-ESSENTIAL — keep locally, not in repo ===

# Hermes plans
.hermes/

# Old TODOs
TODO*.md

# Version control config (standard-version keeps CHANGELOG, but .versionrc is local)
.versionrc

# Test/debug scripts (one-off, not part of build)
backend/test-email.ts
backend/src/migrate-legacy.ts
backend/src/app.controller.spec.ts
backend/src/accounts/accounts.service.spec.ts
backend/src/budgets/budgets.service.spec.ts
backend/src/prisma/prisma.service.spec.ts
backend/test/

# VPS deploy config (keep locally or in secrets)
deploy/

# Server docs that are reference-only
backend/docs/

# Download page (deployed separately)
downloads-index.html
frontend/public/downloads/

# DOCS.md — generated reference, not code
DOCS.md

# CHANGELOG.md — keep tracked for release notes

# CONTRIBUTING.md — keep, it's useful for the repo
# (keeping)

# Version meta — needed by the backend at runtime
# backend/src/version-meta.json — KEEP

# Font file (bundled by Expo at build time, not needed in git)
mobile/assets/fonts/

backend/.env.bak*

# Local security evidence and exploratory study artifacts
engagement/
engagement-*/
dogfood-output/
estudos/
curso-engenharia-ia-aplicada-selmo.md

# Environment files — keep only committed examples/templates
.env*
*.env
*.env.*
!.env.example
!.env.*.example

# Local secrets and signing material
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.mobileprovision
local.properties

# Local databases, dumps and backups
*.db
*.db-*
*.sqlite
*.sqlite3
*.sqlite-*
*.sqlite3-*
*.sql.gz
*.sql.gz.enc
**/backups/
*.bak
*.backup

# Test, coverage and diagnostic output
.nyc_output/
test-results/
blob-report/
*.lcov
junit.xml

# Package-manager and build caches
.npm/
.pnpm-store/
.yarn/
.cache/
*.tsbuildinfo
**/.gradle/
**/build/

# Windows and editor leftovers
Thumbs.db
*.swp
*.swo
